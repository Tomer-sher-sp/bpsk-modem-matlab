The uploaded archive references support/data files that were not included:

- `config.mat`
- `RefInputMod.mat`
- `RefInputAudio.mat`
- a `bitstream2text` helper

Add these files (or replace them with redistributable equivalents) before
claiming that the repository is fully reproducible.
